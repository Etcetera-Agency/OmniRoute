# Implementation Tasks

- [x] `src/lib/fmoPools/inventory.ts` — head snapshot from `getProviderConnections({isActive})` × `getSyncedAvailableModelsForConnection`; exclude tail providers; expand multi-account into account-level rows.
- [x] Enrich rows with models.dev capabilities + `limit_context` + `getModelCompatOverrides` + `freeModelCatalog` free status.
- [x] `src/lib/fmoPools/intelligence.ts` — band resolution via `getResolvedTaskFitness(model, category)`; relax-by-`max_delta`; unrated ⇒ not a head candidate on score.
- [x] `src/lib/fmoPools/quota.ts` — normalize provider-specific `getUsageForProvider(connection)`/quota-cache/reset/per-model shapes into candidate capacity axes; source precedence: normalized live axes (tier 1) → `freeModelCatalog.monthlyTokens` (tier 2) → search-research claim (tier 3) → none (tier 4).
- [x] Relocate search-research (`searchResearchClaim(candidate)`) behind tier 3; implement it as an OmniRoute server-side library helper that calls `buildSearchAttempts`/`runSearchChain` or equivalent internal search chain directly, never `POST /api/v1/search`, `fetch`, `Request`, or the app route boundary.
- [x] Preserve FMO search behavior inside OmniRoute: first attempt `provider="gemini-grounded-search"` with `search_type="web"`, `max_results=10`, and `time_range="month"`; fall back to the default internal search provider only on 429.
- [x] Preserve FMO quota query text inside OmniRoute, including the provider-wide `modelId="*"` topology query, the concrete-model quota query, and the provider-hint omission for `openai-compatible-*` or provider ids longer than 48 characters.
- [x] Consume OmniRoute's normalized search output directly: use `SearchResponse.answer.text` as primary quota text, fall back to `SearchResponse.results[*].snippet`, carry `SearchResponse.results[*].url` as evidence, and attach a search snapshot (`query`, provider used, answer text, snippets, evidence URLs, retrieved time, content hash) to the tier-3 quota result.
- [x] Process the search snapshot with OmniRoute's own internal LLM/chat pipeline using structured JSON output. The extractor must use the existing FMO `quota-research` prompt/rules as the canonical contract and pass the expected variables: `provider`, `provider_model_id`, `source_type`, `source_url`, `text`, and `previous_limit`.
- [x] Do not add a separate FMO/Instructor quota inspector in OmniRoute and do not call an OmniRoute HTTP route for extraction. The internal LLM extractor must return a `QuotaClaimResponse`, then OmniRoute validates evidence/text/range/axis rules and only then trusts + counts the claim as-is.
- [x] `src/lib/fmoPools/capacity.ts` — request-equivalents/day comparator; `tokens_per_request = max(class_weight, global_factor)`; tightest-axis bound.
- [x] Add the global `tokens_per_request` learning loop (observed_tokens/observed_requests, clamped); seed = 2000.
- [x] Tests: multi-account expansion; tail provider excluded from head; in-band vs unrated; live quota adapter handles provider-wide and per-model quota shapes; static figure used without search; class weight cannot understate the global factor.
